# 蜂窝丝滑菜单 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 800×480 SDL 模拟器上实现一个类 Apple Watch 的蜂窝气泡菜单：37 项六边形无界平铺、中心放大镜、拖拽惯性、弹簧吸附、中心高亮、点击回调、滚轮缩放。

**Architecture:** 独立 UI 工程 `hex_menu_ui/` 编成 `lib-ui` 静态库，经模拟器已有的 `LVGL_PRO_PROJECT_DIR` 机制挂载。几何（`hex_grid`）与物理（`hex_physics`）是不依赖 LVGL 的纯函数模块，用裸 gcc 独立测试；`lv_hex_menu.c` 只负责把计算结果映射到 LVGL 对象，用固定大小的对象池覆盖屏幕可见区域并逐帧回收复用。

**Tech Stack:** C99、LVGL 9.6-dev、SDL2、CMake + MinGW-w64 (gcc 16.1.0)

**Spec:** `hex_menu_ui/docs/superpowers/specs/2026-09-07-hex-menu-design.md`

## Global Constraints

- 语言 C99；`hex_grid.*` 与 `hex_physics.*` **禁止 include 任何 LVGL 头文件**，必须能被裸 gcc 单独编译
- 所有新代码用 `-Wall -Wextra -Werror` 编译通过
- 运行期不得有动态内存增长：对象池在初始化时一次性创建，之后只复用不增删
- 平铺周期常量固定为 `HEX_SLOT_CNT = 37`，槽位映射为 `((27*q + r) % 37 + 37) % 37`
- 不使用 `lv_obj_set_style_transform_scale`（会触发 layer 渲染导致掉帧）；气泡缩放一律通过改 `width`/`height` + `LV_RADIUS_CIRCLE` 实现
- 不依赖 LVGL 私有头文件（`*_private.h`）。widget 状态用 `lv_obj_create` + `lv_obj_set_user_data` 挂载，不自定义 `lv_obj_class_t`
- 视觉参数取值必须与 spec 第 6 节表格一致
- 每个任务结束时提交，提交信息用中文，格式 `feat: xxx` / `test: xxx` / `build: xxx`

### 本工程的构建与运行命令（全文复用）

配置（只需在 Task 1 执行一次，之后增量编译不必重跑）：

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake -B build -G "MinGW Makefiles" \
  -DCMAKE_PREFIX_PATH="D:/Program/GUI/SDL2-2.32.10/x86_64-w64-mingw32" \
  -DCMAKE_C_COMPILER="D:/Program/C/mingw64/bin/gcc.exe" \
  -DCMAKE_CXX_COMPILER="D:/Program/C/mingw64/bin/g++.exe" \
  -DCMAKE_MAKE_PROGRAM="D:/Program/C/mingw64/bin/mingw32-make.exe" \
  -DLVGL_PRO_PROJECT_DIR="D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
```

编译与运行：

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake --build build -j
./bin/main.exe
```

**注意**：`cmake --build` 的退出码会被管道吞掉。若要判断成败，用
`cmake --build build -j > /tmp/b.log 2>&1; echo "EXIT=$?"`，不要用 `| tail`。

git 仓库位于 `hex_menu_ui/`（模拟器目录不是 git 仓库）。对 `lv_port_pc_vscode-master/src/main.c` 的改动无法提交，属预期情况，在提交信息里说明即可。

---

### Task 1: 空白 800×480 工程与构建集成

建立独立 UI 工程骨架并挂载到模拟器，跑出一个干净的 800×480 深色空窗口。本任务不含任何蜂窝逻辑。

**Files:**
- Create: `hex_menu_ui/CMakeLists.txt`
- Create: `hex_menu_ui/ui.h`
- Create: `hex_menu_ui/ui.c`
- Create: `hex_menu_ui/.gitignore`
- Modify: `lv_port_pc_vscode-master/src/main.c`（3 处：include、分辨率、入口调用）

**Interfaces:**
- Consumes: 无
- Produces: `void ui_init(void)`（`ui.h`）——模拟器 `main()` 调用的唯一入口；CMake target `lib-ui`

- [ ] **Step 1: 创建 `hex_menu_ui/.gitignore`**

```gitignore
build/
*.exe
*.o
*.a
```

- [ ] **Step 2: 创建 `hex_menu_ui/CMakeLists.txt`**

`lib-ui` 是模拟器顶层 `CMakeLists.txt:128` 写死要求的 target 名，不能改。
`LV_CONF_INCLUDE_SIMPLE` 必须显式加——lvgl target 不会把它传播过来（模拟器顶层对 `main` 也是显式加的，见 `CMakeLists.txt:145`），漏掉会报找不到 `lv_conf.h`。

```cmake
cmake_minimum_required(VERSION 3.12.4)

add_library(lib-ui STATIC
    ui.c
    hex_menu/hex_grid.c
    hex_menu/hex_physics.c
    hex_menu/lv_hex_menu.c
)

target_include_directories(lib-ui PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/hex_menu
)

target_compile_definitions(lib-ui PRIVATE LV_CONF_INCLUDE_SIMPLE)

target_link_libraries(lib-ui PUBLIC lvgl)

if(NOT MSVC)
    target_link_libraries(lib-ui PUBLIC m)
endif()
```

- [ ] **Step 3: 创建四个源文件的最小骨架**

CMakeLists 已列出 4 个 .c 文件，缺任何一个都无法配置成功。本步先建齐，后续任务再填内容。

`hex_menu_ui/ui.h`：

```c
#ifndef UI_H
#define UI_H

#ifdef __cplusplus
extern "C" {
#endif

/** 应用入口：在 lv_init() 与显示初始化之后调用一次 */
void ui_init(void);

#ifdef __cplusplus
}
#endif
#endif /*UI_H*/
```

`hex_menu_ui/ui.c`：

```c
#include "ui.h"
#include "lvgl.h"

void ui_init(void)
{
    lv_obj_t * scr = lv_screen_active();
    lv_obj_set_style_bg_color(scr, lv_color_hex(0x101014), LV_PART_MAIN);
    lv_obj_set_style_bg_opa(scr, LV_OPA_COVER, LV_PART_MAIN);
    lv_obj_remove_flag(scr, LV_OBJ_FLAG_SCROLLABLE);
}
```

`hex_menu_ui/hex_menu/hex_grid.c`、`hex_physics.c`、`lv_hex_menu.c` 各建一个占位翻译单元（ISO C 不允许空文件，必须有至少一个声明）：

```c
/* 占位，内容见 Task 2 */
typedef int hex_grid_placeholder_t;
```

三个文件内容相同，但 typedef 名字要各不相同（`hex_grid_placeholder_t` / `hex_physics_placeholder_t` / `lv_hex_menu_placeholder_t`），避免后续合并时重名。

- [ ] **Step 4: 修改模拟器 `src/main.c`**

三处改动。第一处，在 `#include "hal/hal.h"`（第 27 行）后加一行：

```c
#include "ui.h"
```

第二处，第 64 行分辨率：

```c
  sdl_hal_init(800, 480);
```

第三处，把第 66-72 行的 demo 注释与调用整体替换为：

```c
  /* 启动蜂窝菜单 UI */
  ui_init();
```

`lvgl/examples/lv_examples.h` 与 `lvgl/demos/lv_demos.h` 的 include 保留不动——删掉不会让构建变快，留着也不会被调用。

- [ ] **Step 5: 配置并编译**

运行本文档开头「构建与运行命令」中的 cmake 配置命令，然后：

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake --build build -j > /tmp/b.log 2>&1; echo "EXIT=$?"
```

Expected: `EXIT=0`。配置输出里应出现 `LVGL Pro Project enabled from: .../hex_menu_ui`。
若报 `lv_conf.h` 找不到，说明 Step 2 的 `LV_CONF_INCLUDE_SIMPLE` 漏了。

- [ ] **Step 6: 运行并肉眼验收**

```bash
./bin/main.exe
```

Expected: 弹出 **800×480** 窗口（明显比之前的 320×480 宽），整屏接近纯黑的深色 `#101014`，除鼠标光标外**没有任何控件**。确认后关闭窗口。

- [ ] **Step 7: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "build: 建立独立 UI 工程骨架并挂载到 SDL 模拟器

lib-ui 静态库经 LVGL_PRO_PROJECT_DIR 挂载；模拟器 main.c 改为
800x480 并调用 ui_init()。注意 lv_port_pc_vscode-master 不是 git
仓库，其 main.c 的改动无法纳入本次提交。"
```

---

### Task 2: hex_grid 纯数学模块与测试脚手架

实现六边形轴向坐标与像素的双向转换，以及 37 格环面平铺的槽位映射。建立裸 gcc 测试程序。

**Files:**
- Create: `hex_menu_ui/hex_menu/hex_grid.h`
- Modify: `hex_menu_ui/hex_menu/hex_grid.c`（替换 Task 1 的占位内容）
- Create: `hex_menu_ui/test/test_hex_math.c`

**Interfaces:**
- Consumes: 无
- Produces:
  - `#define HEX_SLOT_CNT 37`
  - `typedef struct { int q; int r; } hex_axial_t;`
  - `void hex_axial_to_px(hex_axial_t a, float s, float * out_x, float * out_y);`
  - `hex_axial_t hex_px_to_axial(float x, float y, float s);`
  - `int hex_slot_index(int q, int r);`

- [ ] **Step 1: 写失败的测试**

创建 `hex_menu_ui/test/test_hex_math.c`。这五条性质就是 spec 第 3.2 节列出的、已用穷举验证过的平铺性质，外加坐标往返。

```c
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "hex_grid.h"

static int g_fail = 0;

#define CHECK(cond, ...)                                              \
    do {                                                              \
        if(!(cond)) {                                                 \
            printf("FAIL %s:%d  ", __FILE__, __LINE__);               \
            printf(__VA_ARGS__);                                      \
            printf("\n");                                             \
            g_fail++;                                                 \
        }                                                             \
    } while(0)

/* 晶格向量：L1 = (a, b) = (4, 3)，L2 = (-b, a+b) = (-3, 7) */
#define L1Q  4
#define L1R  3
#define L2Q (-3)
#define L2R  7

static void test_slot_lattice_vectors_map_to_zero(void)
{
    CHECK(hex_slot_index(0, 0) == 0, "origin -> %d", hex_slot_index(0, 0));
    CHECK(hex_slot_index(L1Q, L1R) == 0, "L1 -> %d", hex_slot_index(L1Q, L1R));
    CHECK(hex_slot_index(L2Q, L2R) == 0, "L2 -> %d", hex_slot_index(L2Q, L2R));
}

static void test_slot_range_and_surjectivity(void)
{
    int seen[HEX_SLOT_CNT];
    int distinct = 0;
    for(int i = 0; i < HEX_SLOT_CNT; i++) seen[i] = 0;

    for(int q = -60; q <= 60; q++) {
        for(int r = -60; r <= 60; r++) {
            int s = hex_slot_index(q, r);
            CHECK(s >= 0 && s < HEX_SLOT_CNT, "slot %d out of range at (%d,%d)", s, q, r);
            if(s >= 0 && s < HEX_SLOT_CNT && seen[s]++ == 0) distinct++;
        }
    }
    CHECK(distinct == HEX_SLOT_CNT, "distinct=%d expected %d", distinct, HEX_SLOT_CNT);
}

static void test_slot_lattice_translation_invariant(void)
{
    srand(1);
    for(int i = 0; i < 200000; i++) {
        int q = rand() % 199 - 99;
        int r = rand() % 199 - 99;
        int m = rand() % 19 - 9;
        int n = rand() % 19 - 9;
        int a = hex_slot_index(q, r);
        int b = hex_slot_index(q + m * L1Q + n * L2Q, r + m * L1R + n * L2R);
        CHECK(a == b, "(%d,%d) m=%d n=%d : %d != %d", q, r, m, n, a, b);
        if(a != b) return;   /* 一条就够，不刷屏 */
    }
}

static void test_radius3_hexagon_is_fundamental_domain(void)
{
    int seen[HEX_SLOT_CNT];
    int cells = 0;
    for(int i = 0; i < HEX_SLOT_CNT; i++) seen[i] = 0;

    for(int q = -3; q <= 3; q++) {
        for(int r = -3; r <= 3; r++) {
            if(abs(q + r) > 3) continue;      /* 轴向坐标下半径 3 的六边形 */
            cells++;
            int s = hex_slot_index(q, r);
            CHECK(seen[s]++ == 0, "duplicate slot %d at (%d,%d)", s, q, r);
        }
    }
    CHECK(cells == HEX_SLOT_CNT, "radius-3 hexagon has %d cells, expected %d", cells, HEX_SLOT_CNT);
}

static void test_px_axial_roundtrip_on_centers(void)
{
    const float s = 80.0f;
    for(int q = -20; q <= 20; q++) {
        for(int r = -20; r <= 20; r++) {
            hex_axial_t in = { q, r };
            float x, y;
            hex_axial_to_px(in, s, &x, &y);
            hex_axial_t out = hex_px_to_axial(x, y, s);
            CHECK(out.q == q && out.r == r,
                  "roundtrip (%d,%d) -> (%.1f,%.1f) -> (%d,%d)", q, r, x, y, out.q, out.r);
            if(out.q != q || out.r != r) return;
        }
    }
}

static void test_px_to_axial_returns_nearest_center(void)
{
    const float s = 80.0f;
    srand(7);
    for(int i = 0; i < 20000; i++) {
        float px = (float)(rand() % 4000 - 2000) * 0.5f;
        float py = (float)(rand() % 4000 - 2000) * 0.5f;

        hex_axial_t got = hex_px_to_axial(px, py, s);
        float gx, gy;
        hex_axial_to_px(got, s, &gx, &gy);
        float best = (px - gx) * (px - gx) + (py - gy) * (py - gy);

        /* 在返回格子周围 2 圈内暴力搜索，不应存在更近的格心 */
        for(int dq = -2; dq <= 2; dq++) {
            for(int dr = -2; dr <= 2; dr++) {
                hex_axial_t cand = { got.q + dq, got.r + dr };
                float cx, cy;
                hex_axial_to_px(cand, s, &cx, &cy);
                float d = (px - cx) * (px - cx) + (py - cy) * (py - cy);
                CHECK(d >= best - 0.01f, "(%.2f,%.2f): (%d,%d) d=%.2f closer than (%d,%d) d=%.2f",
                      px, py, cand.q, cand.r, d, got.q, got.r, best);
                if(d < best - 0.01f) return;
            }
        }
    }
}

int main(void)
{
    test_slot_lattice_vectors_map_to_zero();
    test_slot_range_and_surjectivity();
    test_slot_lattice_translation_invariant();
    test_radius3_hexagon_is_fundamental_domain();
    test_px_axial_roundtrip_on_centers();
    test_px_to_axial_returns_nearest_center();

    if(g_fail == 0) {
        printf("ALL TESTS PASSED\n");
        return 0;
    }
    printf("%d CHECK(s) FAILED\n", g_fail);
    return 1;
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
gcc -O2 -Wall -Wextra -Werror -I hex_menu -o test/test_hex_math.exe \
    test/test_hex_math.c hex_menu/hex_grid.c -lm
```

Expected: 编译失败，`fatal error: hex_grid.h: No such file or directory`。

- [ ] **Step 3: 创建 `hex_menu_ui/hex_menu/hex_grid.h`**

```c
#ifndef HEX_GRID_H
#define HEX_GRID_H

#ifdef __cplusplus
extern "C" {
#endif

/**
 * 平铺周期。37 = 4² + 4·3 + 3² 是 Löschian 数，因此 37 个格子能在无限
 * 六边形网格上构成完美的环面铺砌——朝任意方向拖拽图案都严丝合缝地重复。
 * 半径为 3 的正六边形恰好就是它的基本域。
 */
#define HEX_SLOT_CNT 37

/** pointy-top 轴向坐标 */
typedef struct {
    int q;
    int r;
} hex_axial_t;

/**
 * 轴向坐标 -> 格心像素（世界坐标）
 * @param s 网格特征尺寸（六边形外接圆半径）。列间距 s·√3，行间距 s·1.5
 */
void hex_axial_to_px(hex_axial_t a, float s, float * out_x, float * out_y);

/** 像素（世界坐标）-> 最近的轴向格子 */
hex_axial_t hex_px_to_axial(float x, float y, float s);

/**
 * 轴向坐标 -> 0..HEX_SLOT_CNT-1 的槽位索引。
 * 满足晶格平移不变性：坐标加上 (4,3) 或 (-3,7) 的任意整数倍，返回值不变。
 */
int hex_slot_index(int q, int r);

#ifdef __cplusplus
}
#endif
#endif /*HEX_GRID_H*/
```

- [ ] **Step 4: 实现 `hex_menu_ui/hex_menu/hex_grid.c`**

整体替换 Task 1 的占位内容。

```c
#include "hex_grid.h"
#include <math.h>

#define HEX_SQRT3 1.7320508075688772f

void hex_axial_to_px(hex_axial_t a, float s, float * out_x, float * out_y)
{
    *out_x = s * HEX_SQRT3 * ((float)a.q + (float)a.r * 0.5f);
    *out_y = s * 1.5f * (float)a.r;
}

hex_axial_t hex_px_to_axial(float x, float y, float s)
{
    hex_axial_t out;

    /* hex_axial_to_px 的逆，得到浮点轴向坐标 */
    float rf = y / (s * 1.5f);
    float qf = x / (s * HEX_SQRT3) - rf * 0.5f;

    /* 立方坐标取整：三维分别四舍五入后，把舍入误差最大的一维用另两维反推，
     * 以维持 x + y + z == 0 的约束。直接对 q、r 各自取整会取到错误的格子。 */
    float cx = qf;
    float cz = rf;
    float cy = -cx - cz;

    float rx = roundf(cx);
    float ry = roundf(cy);
    float rz = roundf(cz);

    float dx = fabsf(rx - cx);
    float dy = fabsf(ry - cy);
    float dz = fabsf(rz - cz);

    if(dx > dy && dx > dz)  rx = -ry - rz;
    else if(dy > dz)        ry = -rx - rz;
    else                    rz = -rx - ry;

    (void)ry;   /* ry 只参与上面的修正，不进入返回值 */

    out.q = (int)rx;
    out.r = (int)rz;
    return out;
}

int hex_slot_index(int q, int r)
{
    /* 系数 27 由「两个晶格向量都映射到 0」解出：
     *   4x + 3y ≡ 0 (mod 37)，-3x + 7y ≡ 0 (mod 37)
     *   4⁻¹ mod 37 = 28  =>  x ≡ -3y·28 ≡ -84y ≡ 27y，取 y = 1 得 x = 27
     * 代入第二式验证：-81 + 7 = -74 = -2·37 ≡ 0 ✓ */
    int v = (27 * q + r) % HEX_SLOT_CNT;
    return v < 0 ? v + HEX_SLOT_CNT : v;
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
gcc -O2 -Wall -Wextra -Werror -I hex_menu -o test/test_hex_math.exe \
    test/test_hex_math.c hex_menu/hex_grid.c -lm && ./test/test_hex_math.exe
```

Expected: `ALL TESTS PASSED`，退出码 0。

- [ ] **Step 6: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "feat: hex_grid 六边形坐标与 37 格环面平铺

轴向坐标双向转换（像素->格子用立方坐标取整），槽位映射
index = (27q + r) mod 37。测试覆盖晶格平移不变性、值域穷举、
半径 3 六边形基本域、坐标往返与最近格判定。"
```

---

### Task 3: hex_physics 纯数学模块

实现放大镜曲线、惯性衰减、临界阻尼弹簧。

**Files:**
- Create: `hex_menu_ui/hex_menu/hex_physics.h`
- Modify: `hex_menu_ui/hex_menu/hex_physics.c`（替换 Task 1 的占位内容）
- Modify: `hex_menu_ui/test/test_hex_math.c`（追加测试并在 `main` 中调用）

**Interfaces:**
- Consumes: 无
- Produces:
  - `float hex_smoothstep(float t);`
  - `float hex_magnifier_t(float d, float r);`
  - `float hex_friction_decay(float dt_ms, float tau_ms);`
  - `void hex_spring_step(float * pos, float * vel, float target, float omega, float dt);`

- [ ] **Step 1: 写失败的测试**

在 `test/test_hex_math.c` 顶部的 `#include "hex_grid.h"` 后加一行 `#include "hex_physics.h"`，并在 `main()` 的六个调用之后、`if(g_fail == 0)` 之前插入四个新调用。测试函数追加在 `main()` 之前：

弹簧的过冲断言有一个**前提条件**必须写对：临界阻尼从 `x0` 出发的解是 `x(t) = (A + Bt)e^(-ωt)`，`A = x0`、`B = v0 + ωx0`。只有当 `|v0| > ω|x0|`（初速朝目标且足够大）时 `B` 才会变号导致穿越目标。实际代码里只有 `|v| < HEX_V_SNAP = 12 px/s` 才进入 SNAP，而偏移量最大约半格 70px，`ω|x0| = 12×70 = 840 ≫ 12`，所以前提天然成立。测试必须在这个前提下断言，写成「任意初速都不过冲」是错的，会挂。

```c
static void test_smoothstep_endpoints_and_monotonic(void)
{
    CHECK(hex_smoothstep(-1.0f) == 0.0f, "t<0 -> %f", hex_smoothstep(-1.0f));
    CHECK(hex_smoothstep(0.0f) == 0.0f, "t=0 -> %f", hex_smoothstep(0.0f));
    CHECK(hex_smoothstep(1.0f) == 1.0f, "t=1 -> %f", hex_smoothstep(1.0f));
    CHECK(hex_smoothstep(2.0f) == 1.0f, "t>1 -> %f", hex_smoothstep(2.0f));
    CHECK(fabsf(hex_smoothstep(0.5f) - 0.5f) < 1e-6f, "t=0.5 -> %f", hex_smoothstep(0.5f));

    float prev = -1.0f;
    for(int i = 0; i <= 1000; i++) {
        float t = (float)i / 1000.0f;
        float v = hex_smoothstep(t);
        CHECK(v >= prev, "not monotonic at t=%f: %f < %f", t, v, prev);
        CHECK(v >= 0.0f && v <= 1.0f, "out of [0,1] at t=%f: %f", t, v);
        if(v < prev) return;
        prev = v;
    }
}

static void test_magnifier_t(void)
{
    CHECK(hex_magnifier_t(0.0f, 420.0f) == 0.0f, "d=0 -> %f", hex_magnifier_t(0.0f, 420.0f));
    CHECK(hex_magnifier_t(420.0f, 420.0f) == 1.0f, "d=r -> %f", hex_magnifier_t(420.0f, 420.0f));
    CHECK(hex_magnifier_t(9999.0f, 420.0f) == 1.0f, "d>>r -> %f", hex_magnifier_t(9999.0f, 420.0f));
    CHECK(hex_magnifier_t(100.0f, 0.0f) == 1.0f, "r=0 must not divide by zero");
}

static void test_friction_decays_monotonically_to_zero(void)
{
    float v = 2000.0f;
    float prev = v;
    int frames = 0;
    while(v > 12.0f && frames < 100000) {
        v *= hex_friction_decay(16.0f, 260.0f);
        CHECK(v < prev, "friction not strictly decreasing: %f >= %f", v, prev);
        if(v >= prev) return;
        prev = v;
        frames++;
    }
    CHECK(frames < 100000, "friction never fell below threshold");
    /* tau=260ms 下 2000 -> 12 约需 ln(2000/12)*260/16 ≈ 83 帧 */
    CHECK(frames > 40 && frames < 200, "unexpected settle frames: %d", frames);
}

static void test_spring_converges_without_overshoot(void)
{
    const float omega = 12.0f;
    const float dt = 1.0f / 60.0f;

    srand(11);
    for(int c = 0; c < 500; c++) {
        float x0 = (float)(rand() % 2000 - 1000) * 0.1f;   /* ±100 px */
        if(fabsf(x0) < 1.0f) continue;
        /* 前提：|v0| <= omega*|x0|，由 HEX_V_SNAP 阈值保证 */
        float vmax = omega * fabsf(x0);
        float v0 = ((float)(rand() % 2001 - 1000) / 1000.0f) * vmax;

        float pos = x0, vel = v0;
        float worst_overshoot = 0.0f;
        for(int i = 0; i < 2000; i++) {
            hex_spring_step(&pos, &vel, 0.0f, omega, dt);
            /* 过冲 = 越过目标(0)到达的反向距离 */
            float os = (x0 > 0.0f) ? -pos : pos;
            if(os > worst_overshoot) worst_overshoot = os;
        }
        CHECK(fabsf(pos) < 0.01f, "did not converge: x0=%f v0=%f -> %f", x0, v0, pos);
        CHECK(worst_overshoot <= fabsf(x0) * 0.02f,
              "overshoot %f exceeds 2%% of |x0|=%f", worst_overshoot, fabsf(x0));
        if(fabsf(pos) >= 0.01f || worst_overshoot > fabsf(x0) * 0.02f) return;
    }
}
```

在 `main()` 中追加：

```c
    test_smoothstep_endpoints_and_monotonic();
    test_magnifier_t();
    test_friction_decays_monotonically_to_zero();
    test_spring_converges_without_overshoot();
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
gcc -O2 -Wall -Wextra -Werror -I hex_menu -o test/test_hex_math.exe \
    test/test_hex_math.c hex_menu/hex_grid.c hex_menu/hex_physics.c -lm
```

Expected: 编译失败，`fatal error: hex_physics.h: No such file or directory`。

- [ ] **Step 3: 创建 `hex_menu_ui/hex_menu/hex_physics.h`**

```c
#ifndef HEX_PHYSICS_H
#define HEX_PHYSICS_H

#ifdef __cplusplus
extern "C" {
#endif

/**
 * smoothstep。t<=0 返回 0，t>=1 返回 1，区间内单调递增且两端一阶导为 0。
 * 选它而非高斯是为了 MCU 友好：只有乘加，无超越函数。
 */
float hex_smoothstep(float t);

/**
 * 放大镜插值系数。
 * @param d 气泡离焦点的距离（未经径向压缩的世界坐标距离）
 * @param r 影响半径
 * @return 0（正处于中心）~ 1（在影响半径之外）
 */
float hex_magnifier_t(float d, float r);

/**
 * 惯性衰减系数。用时间常数而非每帧固定系数，掉帧时手感不变。
 * @param dt_ms  本帧耗时（毫秒）
 * @param tau_ms 时间常数（毫秒）
 * @return 速度应乘上的系数，位于 (0, 1]
 */
float hex_friction_decay(float dt_ms, float tau_ms);

/**
 * 临界阻尼弹簧的一步积分，就地更新 pos 与 vel。
 * 用半隐式欧拉（先更新速度再更新位置），比显式欧拉稳定得多。
 * @param omega 角频率（rad/s）
 * @param dt    本帧耗时（秒）
 */
void hex_spring_step(float * pos, float * vel, float target, float omega, float dt);

#ifdef __cplusplus
}
#endif
#endif /*HEX_PHYSICS_H*/
```

- [ ] **Step 4: 实现 `hex_menu_ui/hex_menu/hex_physics.c`**

整体替换 Task 1 的占位内容。

```c
#include "hex_physics.h"
#include <math.h>

float hex_smoothstep(float t)
{
    if(t <= 0.0f) return 0.0f;
    if(t >= 1.0f) return 1.0f;
    return t * t * (3.0f - 2.0f * t);
}

float hex_magnifier_t(float d, float r)
{
    if(r <= 0.0f) return 1.0f;
    return hex_smoothstep(d / r);
}

float hex_friction_decay(float dt_ms, float tau_ms)
{
    if(tau_ms <= 0.0f) return 0.0f;
    return expf(-dt_ms / tau_ms);
}

void hex_spring_step(float * pos, float * vel, float target, float omega, float dt)
{
    /* 临界阻尼：k = ω²，c = 2ω。阻尼比恰为 1，不振荡且收敛最快。 */
    float k = omega * omega;
    float c = 2.0f * omega;
    float a = -k * (*pos - target) - c * (*vel);

    *vel += a * dt;          /* 半隐式欧拉：先速度 */
    *pos += (*vel) * dt;     /* 再用新速度更新位置 */
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
gcc -O2 -Wall -Wextra -Werror -I hex_menu -o test/test_hex_math.exe \
    test/test_hex_math.c hex_menu/hex_grid.c hex_menu/hex_physics.c -lm \
    && ./test/test_hex_math.exe
```

Expected: `ALL TESTS PASSED`，退出码 0（含 Task 2 的 6 个测试，共 10 个）。

- [ ] **Step 6: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "feat: hex_physics 放大镜曲线与惯性弹簧

smoothstep 放大镜插值、基于时间常数的惯性衰减、半隐式欧拉积分的
临界阻尼弹簧。过冲测试在 |v0| <= omega*|x0| 前提下断言，该前提由
运行期的 V_SNAP 阈值保证。"
```

---

### Task 4: 蜂窝静态渲染（对象池 + 布局 + 放大镜）

创建 widget，建立对象池，按固定焦点渲染出 37 项蜂窝与放大镜效果。**本任务不含任何交互**——焦点写死在原点不动。

**Files:**
- Create: `hex_menu_ui/hex_menu/lv_hex_menu.h`
- Modify: `hex_menu_ui/hex_menu/lv_hex_menu.c`（替换 Task 1 的占位内容）
- Modify: `hex_menu_ui/ui.c`

**Interfaces:**
- Consumes: `hex_grid.h` 与 `hex_physics.h` 的全部导出
- Produces:
  - `typedef struct { const char * icon; const char * label; lv_color_t color; } lv_hex_menu_item_t;`
  - `lv_obj_t * lv_hex_menu_create(lv_obj_t * parent);`
  - `void lv_hex_menu_set_items(lv_obj_t * obj, const lv_hex_menu_item_t * items, uint32_t cnt);`
  - 内部（Task 5/6/7 会用）：`hex_menu_ctx_t` 状态结构、`static hex_menu_ctx_t * ctx_of(lv_obj_t * obj)`、`static void hex_layout(lv_obj_t * obj)`

- [ ] **Step 1: 创建 `hex_menu_ui/hex_menu/lv_hex_menu.h`**

```c
#ifndef LV_HEX_MENU_H
#define LV_HEX_MENU_H

#include "lvgl.h"

#ifdef __cplusplus
extern "C" {
#endif

typedef struct {
    const char * icon;    /**< LV_SYMBOL_* 或任意 UTF-8 文本 */
    const char * label;   /**< 保留给第二阶段的展开面板，当前不绘制 */
    lv_color_t   color;   /**< 圆底色 */
} lv_hex_menu_item_t;

/** 创建蜂窝菜单。默认填满父对象。 */
lv_obj_t * lv_hex_menu_create(lv_obj_t * parent);

/**
 * 设置菜单项。items 数组由调用方保证生命周期长于 widget（不做拷贝）。
 * 平铺周期固定 37 槽；cnt < 37 时按 items[slot % cnt] 映射，一个周期内会重复。
 * cnt == 37 时铺砌完美无重复，是设计目标场景。
 */
void lv_hex_menu_set_items(lv_obj_t * obj, const lv_hex_menu_item_t * items, uint32_t cnt);

/** 当前离屏幕中心最近的菜单项索引，0..cnt-1；未设置菜单项时返回 -1 */
int32_t lv_hex_menu_get_focused(lv_obj_t * obj);

/** 点击菜单项时触发。回调中用 lv_event_get_user_data() 取 user_data。 */
void lv_hex_menu_set_event_cb(lv_obj_t * obj, lv_event_cb_t cb, void * user_data);

#ifdef __cplusplus
}
#endif
#endif /*LV_HEX_MENU_H*/
```

- [ ] **Step 2: 实现 `lv_hex_menu.c` 的骨架、参数与对象池**

整体替换 Task 1 的占位内容。本步只到「静态渲染」，交互留给后续任务。

不自定义 `lv_obj_class_t`：它的结构体定义在私有头 `src/core/lv_obj_class_private.h` 里，用了就得依赖 LVGL 私有 API。改用 `lv_obj_create` + `user_data`，在 `LV_EVENT_DELETE` 里释放。

```c
#include "lv_hex_menu.h"
#include "hex_grid.h"
#include "hex_physics.h"
#include <math.h>

/* ------------------ 可调参数（对应 spec 第 6 节） ------------------ */
#define HEX_S_DEF            80.0f    /* 网格尺寸默认值 */
#define HEX_S_MIN            60.0f
#define HEX_S_MAX           120.0f
#define HEX_D_MAX           120.0f    /* 中心气泡直径 */
#define HEX_D_MIN            44.0f    /* 最远气泡直径 */
#define HEX_R_INFLUENCE     420.0f    /* 放大镜影响半径 */
#define HEX_OPA_MAX           255
#define HEX_OPA_MIN            90
#define HEX_RADIAL_COMPRESS   0.12f   /* 径向位移压缩系数 */
#define HEX_HL_COLOR          0xFF9500

#define HEX_POOL_SIZE         128     /* S_MIN 时最坏约需 108，留余量 */
#define HEX_PERIOD_MS          16
#define HEX_SQRT3             1.7320508075688772f

/* Montserrat 12..48 步进 2，共 19 档。lv_conf.h 中已全部开启。 */
static const lv_font_t * const HEX_FONTS[] = {
    &lv_font_montserrat_12, &lv_font_montserrat_14, &lv_font_montserrat_16,
    &lv_font_montserrat_18, &lv_font_montserrat_20, &lv_font_montserrat_22,
    &lv_font_montserrat_24, &lv_font_montserrat_26, &lv_font_montserrat_28,
    &lv_font_montserrat_30, &lv_font_montserrat_32, &lv_font_montserrat_34,
    &lv_font_montserrat_36, &lv_font_montserrat_38, &lv_font_montserrat_40,
    &lv_font_montserrat_42, &lv_font_montserrat_44, &lv_font_montserrat_46,
    &lv_font_montserrat_48,
};
#define HEX_FONT_CNT ((int32_t)(sizeof(HEX_FONTS) / sizeof(HEX_FONTS[0])))

/* ------------------ 内部类型 ------------------ */

typedef struct {
    lv_obj_t * bubble;
    lv_obj_t * icon;
    int32_t    slot;        /* 当前占用槽位，-1 表示本帧未使用 */
    int32_t    last_d;      /* 上次直径，用于跳过无变化的 setter */
    int32_t    last_x;
    int32_t    last_y;
    int32_t    last_font;   /* 上次字体档位索引 */
    lv_opa_t   last_opa;
} hex_cell_t;

typedef struct {
    const lv_hex_menu_item_t * items;
    uint32_t                   item_cnt;

    hex_cell_t pool[HEX_POOL_SIZE];

    float fx, fy;      /* 焦点世界坐标 */
    float vx, vy;      /* 速度 px/s */
    float s;           /* 当前网格尺寸 */

    int32_t focus_slot;

    lv_timer_t * timer;
    uint32_t     last_tick;
} hex_menu_ctx_t;

static hex_menu_ctx_t * ctx_of(lv_obj_t * obj)
{
    return (hex_menu_ctx_t *)lv_obj_get_user_data(obj);
}

/* ------------------ 工具 ------------------ */

static int32_t font_idx_for_diam(int32_t diam)
{
    int32_t idx = ((diam * 42) / 100 - 12) / 2;   /* 字号 ≈ 直径 × 0.42 */
    if(idx < 0) idx = 0;
    if(idx >= HEX_FONT_CNT) idx = HEX_FONT_CNT - 1;
    return idx;
}

static const lv_hex_menu_item_t * item_of_slot(hex_menu_ctx_t * m, int32_t slot)
{
    if(m->items == NULL || m->item_cnt == 0) return NULL;
    return &m->items[(uint32_t)slot % m->item_cnt];
}

/* ------------------ 布局 ------------------ */

static void hex_layout(lv_obj_t * obj)
{
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    const int32_t w = lv_obj_get_width(obj);
    const int32_t h = lv_obj_get_height(obj);
    const float cx = (float)w * 0.5f;
    const float cy = (float)h * 0.5f;

    /* 径向压缩会把远处的气泡拉进视野，所以搜索范围要按压缩前放大 */
    const float margin = 1.0f / (1.0f - HEX_RADIAL_COMPRESS);
    const float half_w = cx * margin + m->s;
    const float half_h = cy * margin + m->s;

    int used = 0;
    float best_d2 = 1e30f;
    int32_t best_slot = -1;

    const int r0 = (int)floorf((m->fy - half_h) / (m->s * 1.5f));
    const int r1 = (int)ceilf((m->fy + half_h) / (m->s * 1.5f));

    for(int r = r0; r <= r1 && used < HEX_POOL_SIZE; r++) {
        const int q0 = (int)floorf((m->fx - half_w) / (m->s * HEX_SQRT3) - (float)r * 0.5f);
        const int q1 = (int)ceilf((m->fx + half_w) / (m->s * HEX_SQRT3) - (float)r * 0.5f);

        for(int q = q0; q <= q1 && used < HEX_POOL_SIZE; q++) {
            hex_axial_t a = { q, r };
            float wx, wy;
            hex_axial_to_px(a, m->s, &wx, &wy);

            /* 相对焦点的偏移。距离必须用未压缩值，否则屏幕坐标与压缩系数互相
             * 依赖会形成循环。 */
            const float ox = wx - m->fx;
            const float oy = wy - m->fy;
            const float d = sqrtf(ox * ox + oy * oy);

            const float t = hex_magnifier_t(d, HEX_R_INFLUENCE);
            const float diam_f = HEX_D_MAX + (HEX_D_MIN - HEX_D_MAX) * t;
            const float shrink = 1.0f - HEX_RADIAL_COMPRESS * t;

            const int32_t sx = (int32_t)lroundf(cx + ox * shrink);
            const int32_t sy = (int32_t)lroundf(cy + oy * shrink);
            const int32_t diam = (int32_t)lroundf(diam_f);

            /* 完全在屏幕外的不占用池对象 */
            const int32_t half = diam / 2 + 1;
            if(sx + half < 0 || sx - half > w || sy + half < 0 || sy - half > h) continue;

            const int32_t slot = hex_slot_index(q, r);
            const float d2 = ox * ox + oy * oy;
            if(d2 < best_d2) {
                best_d2 = d2;
                best_slot = slot;
            }

            hex_cell_t * c = &m->pool[used++];
            const lv_hex_menu_item_t * it = item_of_slot(m, slot);
            const lv_opa_t opa = (lv_opa_t)(HEX_OPA_MAX + (HEX_OPA_MIN - HEX_OPA_MAX) * t);

            if(c->slot != slot) {
                c->slot = slot;
                if(it != NULL) {
                    lv_obj_set_style_bg_color(c->bubble, it->color, LV_PART_MAIN);
                    lv_label_set_text(c->icon, it->icon);
                }
            }

            if(c->last_d != diam) {
                c->last_d = diam;
                lv_obj_set_size(c->bubble, diam, diam);

                const int32_t fi = font_idx_for_diam(diam);
                if(c->last_font != fi) {
                    c->last_font = fi;
                    lv_obj_set_style_text_font(c->icon, HEX_FONTS[fi], LV_PART_MAIN);
                }
            }

            const int32_t px = sx - diam / 2;
            const int32_t py = sy - diam / 2;
            if(c->last_x != px || c->last_y != py) {
                c->last_x = px;
                c->last_y = py;
                lv_obj_set_pos(c->bubble, px, py);
            }

            if(c->last_opa != opa) {
                c->last_opa = opa;
                lv_obj_set_style_bg_opa(c->bubble, opa, LV_PART_MAIN);
                lv_obj_set_style_text_opa(c->icon, opa, LV_PART_MAIN);
            }
            lv_obj_remove_flag(c->bubble, LV_OBJ_FLAG_HIDDEN);
        }
    }

    m->focus_slot = best_slot;

    for(int i = used; i < HEX_POOL_SIZE; i++) {
        m->pool[i].slot = -1;
        lv_obj_add_flag(m->pool[i].bubble, LV_OBJ_FLAG_HIDDEN);
    }
}
```

- [ ] **Step 3: 实现创建、销毁与定时器**

接在 Step 2 的代码之后。

```c
/* ------------------ 定时器 ------------------ */

static void hex_timer_cb(lv_timer_t * t)
{
    lv_obj_t * obj = (lv_obj_t *)lv_timer_get_user_data(t);
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    const uint32_t now = lv_tick_get();
    float dt_ms = (float)(now - m->last_tick);
    m->last_tick = now;

    if(dt_ms <= 0.0f) return;
    if(dt_ms > 100.0f) dt_ms = 100.0f;   /* 掉帧保护，避免一帧飞出天际 */

    /* Task 5 会在此处插入物理状态机 */
    hex_layout(obj);
}

/* ------------------ 生命周期 ------------------ */

static void hex_delete_cb(lv_event_t * e)
{
    lv_obj_t * obj = lv_event_get_target_obj(e);
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    if(m->timer != NULL) lv_timer_delete(m->timer);
    lv_obj_set_user_data(obj, NULL);
    lv_free(m);
}

lv_obj_t * lv_hex_menu_create(lv_obj_t * parent)
{
    lv_obj_t * obj = lv_obj_create(parent);
    lv_obj_set_size(obj, LV_PCT(100), LV_PCT(100));
    lv_obj_set_style_bg_opa(obj, LV_OPA_TRANSP, LV_PART_MAIN);
    lv_obj_set_style_border_width(obj, 0, LV_PART_MAIN);
    lv_obj_set_style_pad_all(obj, 0, LV_PART_MAIN);
    lv_obj_remove_flag(obj, LV_OBJ_FLAG_SCROLLABLE);

    hex_menu_ctx_t * m = (hex_menu_ctx_t *)lv_malloc_zeroed(sizeof(hex_menu_ctx_t));
    LV_ASSERT_MALLOC(m);
    if(m == NULL) return obj;

    m->s = HEX_S_DEF;
    m->focus_slot = -1;
    lv_obj_set_user_data(obj, m);

    for(int i = 0; i < HEX_POOL_SIZE; i++) {
        hex_cell_t * c = &m->pool[i];

        c->bubble = lv_obj_create(obj);
        lv_obj_remove_flag(c->bubble, LV_OBJ_FLAG_CLICKABLE);   /* 事件交给根对象统一处理 */
        lv_obj_remove_flag(c->bubble, LV_OBJ_FLAG_SCROLLABLE);
        lv_obj_add_flag(c->bubble, LV_OBJ_FLAG_HIDDEN);
        lv_obj_set_style_radius(c->bubble, LV_RADIUS_CIRCLE, LV_PART_MAIN);
        lv_obj_set_style_border_width(c->bubble, 0, LV_PART_MAIN);
        lv_obj_set_style_pad_all(c->bubble, 0, LV_PART_MAIN);
        lv_obj_set_size(c->bubble, (int32_t)HEX_D_MIN, (int32_t)HEX_D_MIN);

        c->icon = lv_label_create(c->bubble);
        lv_obj_center(c->icon);
        lv_obj_set_style_text_color(c->icon, lv_color_white(), LV_PART_MAIN);
        lv_label_set_text(c->icon, "");

        c->slot = -1;
        c->last_d = -1;
        c->last_x = INT32_MIN;
        c->last_y = INT32_MIN;
        c->last_font = -1;
        c->last_opa = 0;
    }

    lv_obj_add_event_cb(obj, hex_delete_cb, LV_EVENT_DELETE, NULL);

    m->last_tick = lv_tick_get();
    m->timer = lv_timer_create(hex_timer_cb, HEX_PERIOD_MS, obj);

    return obj;
}

void lv_hex_menu_set_items(lv_obj_t * obj, const lv_hex_menu_item_t * items, uint32_t cnt)
{
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    m->items = items;
    m->item_cnt = cnt;

    for(int i = 0; i < HEX_POOL_SIZE; i++) m->pool[i].slot = -1;   /* 强制刷新内容 */
    hex_layout(obj);
}

int32_t lv_hex_menu_get_focused(lv_obj_t * obj)
{
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL || m->items == NULL || m->item_cnt == 0 || m->focus_slot < 0) return -1;
    return (int32_t)((uint32_t)m->focus_slot % m->item_cnt);
}

void lv_hex_menu_set_event_cb(lv_obj_t * obj, lv_event_cb_t cb, void * user_data)
{
    /* Task 6 实现 */
    LV_UNUSED(obj);
    LV_UNUSED(cb);
    LV_UNUSED(user_data);
}
```

- [ ] **Step 4: 在 `ui.c` 里装配 37 个菜单项**

整体替换 `ui.c`。色相在 HSV 上均匀分布，得到 37 个区分度足够的圆底色。

```c
#include "ui.h"
#include "lvgl.h"
#include "lv_hex_menu.h"

#define UI_ITEM_CNT 37

static lv_hex_menu_item_t s_items[UI_ITEM_CNT];

static const char * const s_icons[UI_ITEM_CNT] = {
    LV_SYMBOL_HOME,      LV_SYMBOL_SETTINGS,  LV_SYMBOL_BELL,      LV_SYMBOL_WIFI,
    LV_SYMBOL_BLUETOOTH, LV_SYMBOL_BATTERY_FULL, LV_SYMBOL_CHARGE, LV_SYMBOL_USB,
    LV_SYMBOL_SD_CARD,   LV_SYMBOL_SAVE,      LV_SYMBOL_DOWNLOAD,  LV_SYMBOL_UPLOAD,
    LV_SYMBOL_REFRESH,   LV_SYMBOL_COPY,      LV_SYMBOL_PASTE,     LV_SYMBOL_TRASH,
    LV_SYMBOL_EDIT,      LV_SYMBOL_CUT,       LV_SYMBOL_KEYBOARD,  LV_SYMBOL_GPS,
    LV_SYMBOL_CALL,      LV_SYMBOL_ENVELOPE,  LV_SYMBOL_IMAGE,     LV_SYMBOL_VIDEO,
    LV_SYMBOL_AUDIO,     LV_SYMBOL_PLAY,      LV_SYMBOL_PAUSE,     LV_SYMBOL_STOP,
    LV_SYMBOL_NEXT,      LV_SYMBOL_PREV,      LV_SYMBOL_VOLUME_MAX,LV_SYMBOL_LIST,
    LV_SYMBOL_FILE,      LV_SYMBOL_DIRECTORY, LV_SYMBOL_EYE_OPEN,  LV_SYMBOL_POWER,
    LV_SYMBOL_OK,
};

static const char * const s_labels[UI_ITEM_CNT] = {
    "Home",     "Settings", "Alerts",   "Wi-Fi",    "Bluetooth","Battery",  "Charge",
    "USB",      "SD Card",  "Save",     "Download", "Upload",   "Refresh",  "Copy",
    "Paste",    "Trash",    "Edit",     "Cut",      "Keyboard", "GPS",      "Call",
    "Mail",     "Photos",   "Video",    "Audio",    "Play",     "Pause",    "Stop",
    "Next",     "Prev",     "Volume",   "List",     "Files",    "Folders",  "Preview",
    "Power",    "Done",
};

void ui_init(void)
{
    lv_obj_t * scr = lv_screen_active();
    lv_obj_set_style_bg_color(scr, lv_color_hex(0x101014), LV_PART_MAIN);
    lv_obj_set_style_bg_opa(scr, LV_OPA_COVER, LV_PART_MAIN);
    lv_obj_remove_flag(scr, LV_OBJ_FLAG_SCROLLABLE);

    for(uint32_t i = 0; i < UI_ITEM_CNT; i++) {
        s_items[i].icon  = s_icons[i];
        s_items[i].label = s_labels[i];
        /* 色相均匀铺开；饱和度与亮度固定，保证白色图标始终可读 */
        s_items[i].color = lv_color_hsv_to_rgb((uint16_t)(i * 360u / UI_ITEM_CNT), 62, 88);
    }

    lv_obj_t * menu = lv_hex_menu_create(scr);
    lv_hex_menu_set_items(menu, s_items, UI_ITEM_CNT);
}
```

- [ ] **Step 5: 编译并肉眼验收**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake --build build -j > /tmp/b.log 2>&1; echo "EXIT=$?"
./bin/main.exe
```

Expected：
- 满屏彩色圆形气泡，按六边形蜂窝排布（相邻行错开半格），不重叠
- 屏幕中心的气泡最大（约 120px）、最不透明；越靠边越小（最小约 44px）、越淡
- 每个气泡中心有一个白色 symbol 图标，大小随气泡变化
- 画面静止不动（本任务尚无交互）

若鼠标光标被气泡挡住看不见：`hal.c` 先于菜单创建了光标图像对象，在 `ui_init()` 末尾对其调用 `lv_obj_move_foreground()` 即可，但这不属于本任务范围，记录下来交给 Task 6 处理。

- [ ] **Step 6: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "feat: 蜂窝静态渲染，对象池 + 放大镜

固定 128 个对象的池覆盖可见区并逐帧回收复用；缩放走 width/height +
LV_RADIUS_CIRCLE，图标按直径就近选 Montserrat 字号档，全程不用
transform_scale。状态挂在 user_data 上，不依赖 LVGL 私有 API。"
```

---

### Task 5: 拖拽、惯性与弹簧吸附

给菜单加上三态状态机，实现二维无界拖拽、松手惯性滑行、静止前弹簧吸附到最近格。

**Files:**
- Modify: `hex_menu_ui/hex_menu/lv_hex_menu.c`

**Interfaces:**
- Consumes: Task 4 的 `hex_menu_ctx_t`、`ctx_of()`、`hex_layout()`；Task 3 的 `hex_friction_decay()`、`hex_spring_step()`；Task 2 的 `hex_px_to_axial()`、`hex_axial_to_px()`
- Produces: `hex_menu_ctx_t` 新增字段 `state`、`last_pt`、`press_pt`、`press_tick`、`press_travel`、`target_x`、`target_y`（Task 6 要用 `press_travel` 与 `press_tick` 判点击）

- [ ] **Step 1: 扩充状态结构与参数**

在 `lv_hex_menu.c` 的参数区追加：

```c
#define HEX_TAU_MS          260.0f   /* 惯性时间常数 */
#define HEX_V_SNAP           12.0f   /* 低于此速度转入吸附，px/s */
#define HEX_OMEGA            12.0f   /* 弹簧角频率 rad/s */
#define HEX_VEL_EMA           0.35f  /* 拖拽测速的指数滑动平均系数 */
```

在 `hex_cell_t` 之后、`hex_menu_ctx_t` 之前插入：

```c
typedef enum {
    HEX_ST_IDLE,
    HEX_ST_DRAG,
    HEX_ST_GLIDE,
    HEX_ST_SNAP,
} hex_state_t;
```

在 `hex_menu_ctx_t` 的 `int32_t focus_slot;` 之前插入：

```c
    hex_state_t state;
    lv_point_t  last_pt;      /* 上一帧指针位置 */
    lv_point_t  press_pt;     /* 按下时的指针位置 */
    uint32_t    press_tick;
    int32_t     press_travel; /* 按下期间累计位移，用于区分点击与拖拽 */
    float       target_x;     /* 吸附目标 */
    float       target_y;
```

- [ ] **Step 2: 实现状态机**

插在 `hex_timer_cb` 之前。

```c
/* ------------------ 物理状态机 ------------------ */

static void hex_pick_snap_target(hex_menu_ctx_t * m)
{
    hex_axial_t a = hex_px_to_axial(m->fx, m->fy, m->s);
    hex_axial_to_px(a, m->s, &m->target_x, &m->target_y);
}

static void hex_physics_update(hex_menu_ctx_t * m, float dt, float dt_ms)
{
    switch(m->state) {
        case HEX_ST_DRAG:
            /* 位置在事件回调里直接跟随指针，这里不动 */
            break;

        case HEX_ST_GLIDE: {
            m->fx += m->vx * dt;
            m->fy += m->vy * dt;

            const float decay = hex_friction_decay(dt_ms, HEX_TAU_MS);
            m->vx *= decay;
            m->vy *= decay;

            const float speed = sqrtf(m->vx * m->vx + m->vy * m->vy);
            if(speed < HEX_V_SNAP) {
                hex_pick_snap_target(m);
                m->state = HEX_ST_SNAP;
            }
            break;
        }

        case HEX_ST_SNAP: {
            hex_spring_step(&m->fx, &m->vx, m->target_x, HEX_OMEGA, dt);
            hex_spring_step(&m->fy, &m->vy, m->target_y, HEX_OMEGA, dt);

            const float dx = m->fx - m->target_x;
            const float dy = m->fy - m->target_y;
            if(dx * dx + dy * dy < 0.25f &&
               m->vx * m->vx + m->vy * m->vy < 1.0f) {
                m->fx = m->target_x;
                m->fy = m->target_y;
                m->vx = 0.0f;
                m->vy = 0.0f;
                m->state = HEX_ST_IDLE;
            }
            break;
        }

        case HEX_ST_IDLE:
        default:
            break;
    }
}
```

- [ ] **Step 3: 在定时器里接上状态机**

把 `hex_timer_cb` 里 `/* Task 5 会在此处插入物理状态机 */` 那一行替换为：

```c
    hex_physics_update(m, dt_ms * 0.001f, dt_ms);
```

- [ ] **Step 4: 实现指针事件**

插在 `hex_delete_cb` 之前。

拖拽方向必须**取反**：手指向右拖，是要把左边的内容拉过来，所以焦点在世界坐标里向左移。

```c
/* ------------------ 指针事件 ------------------ */

static void hex_pointer_cb(lv_event_t * e)
{
    lv_obj_t * obj = lv_event_get_target_obj(e);
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    lv_indev_t * indev = lv_indev_active();
    if(indev == NULL) return;

    lv_point_t p;
    lv_indev_get_point(indev, &p);

    switch(lv_event_get_code(e)) {
        case LV_EVENT_PRESSED:
            m->state = HEX_ST_DRAG;
            m->vx = 0.0f;
            m->vy = 0.0f;
            m->last_pt = p;
            m->press_pt = p;
            m->press_tick = lv_tick_get();
            m->press_travel = 0;
            break;

        case LV_EVENT_PRESSING: {
            const int32_t dx = p.x - m->last_pt.x;
            const int32_t dy = p.y - m->last_pt.y;
            if(dx == 0 && dy == 0) break;

            m->press_travel += LV_ABS(dx) + LV_ABS(dy);

            /* 指针右移 => 焦点在世界坐标里左移 */
            m->fx -= (float)dx;
            m->fy -= (float)dy;

            /* 用指数滑动平均测速，直接用单帧增量会把松手瞬间的抖动放大成乱飞 */
            const uint32_t now = lv_tick_get();
            float dt_ms = (float)(now - m->last_tick);
            if(dt_ms < 1.0f) dt_ms = 1.0f;

            const float inst_vx = -(float)dx * 1000.0f / dt_ms;
            const float inst_vy = -(float)dy * 1000.0f / dt_ms;
            m->vx += (inst_vx - m->vx) * HEX_VEL_EMA;
            m->vy += (inst_vy - m->vy) * HEX_VEL_EMA;

            m->last_pt = p;
            break;
        }

        case LV_EVENT_RELEASED:
            m->state = HEX_ST_GLIDE;
            break;

        default:
            break;
    }
}
```

- [ ] **Step 5: 注册事件并让根对象可点**

在 `lv_hex_menu_create()` 中，`lv_obj_remove_flag(obj, LV_OBJ_FLAG_SCROLLABLE);` 之后补一行：

```c
    lv_obj_add_flag(obj, LV_OBJ_FLAG_CLICKABLE);
```

并在 `lv_obj_add_event_cb(obj, hex_delete_cb, LV_EVENT_DELETE, NULL);` 之前插入三行：

```c
    lv_obj_add_event_cb(obj, hex_pointer_cb, LV_EVENT_PRESSED, NULL);
    lv_obj_add_event_cb(obj, hex_pointer_cb, LV_EVENT_PRESSING, NULL);
    lv_obj_add_event_cb(obj, hex_pointer_cb, LV_EVENT_RELEASED, NULL);
```

- [ ] **Step 6: 编译并肉眼验收**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake --build build -j > /tmp/b.log 2>&1; echo "EXIT=$?"
./bin/main.exe
```

Expected：
- 按住鼠标拖动，蜂窝跟手移动，方向与手一致（右拖内容右移）
- 朝任意方向持续拖动**永远拖不到头**，图案周期重复且无接缝、无错位
- 快速甩动后松手，蜂窝继续滑行并平滑减速
- 停下前会轻微「吸」一下，最终有一个气泡精确停在屏幕正中
- 吸附过程不来回振荡

- [ ] **Step 7: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "feat: 二维无界拖拽、惯性滑行与弹簧吸附

DRAG/GLIDE/SNAP 三态机。拖拽测速用指数滑动平均，避免松手抖动被
放大；惯性按时间常数衰减，掉帧时手感不变；吸附用临界阻尼弹簧。"
```

---

### Task 6: 中心高亮与点击回调

**Files:**
- Modify: `hex_menu_ui/hex_menu/lv_hex_menu.c`
- Modify: `hex_menu_ui/ui.c`

**Interfaces:**
- Consumes: Task 5 的 `press_travel`、`press_tick`、`hex_menu_ctx_t.pool`
- Produces: `lv_hex_menu_set_event_cb()` 的真实实现；回调以 `LV_EVENT_VALUE_CHANGED` 触发，被点项索引经 `lv_event_get_param()` 传出（`int32_t` 转 `void *`）

- [ ] **Step 1: 扩充状态与参数**

参数区追加：

```c
#define HEX_CLICK_TRAVEL       8    /* 累计位移小于此值才算点击，px */
#define HEX_CLICK_MS         400    /* 按下时长小于此值才算点击 */
#define HEX_PULSE_MS         180    /* 点击/跨格的缩放脉冲时长 */
#define HEX_PULSE_GAIN         0.08f /* 脉冲峰值放大比例 */
```

`hex_menu_ctx_t` 追加：

```c
    lv_event_cb_t user_cb;
    void *        user_data;
    int32_t       pulse_slot;    /* 正在脉冲的槽位，-1 表示无 */
    uint32_t      pulse_tick;    /* 脉冲起始时刻 */
```

- [ ] **Step 2: 布局中加入高亮与脉冲**

在 `hex_layout()` 里，把这一段：

```c
            if(c->slot != slot) {
                c->slot = slot;
                if(it != NULL) {
                    lv_obj_set_style_bg_color(c->bubble, it->color, LV_PART_MAIN);
                    lv_label_set_text(c->icon, it->icon);
                }
            }
```

替换为（新增高亮判定，且颜色随高亮状态变化，故不能只在换槽位时刷新）：

```c
            const bool hl = (slot == m->focus_slot_prev);
            if(c->slot != slot) {
                c->slot = slot;
                if(it != NULL) lv_label_set_text(c->icon, it->icon);
                c->last_hl = -1;      /* 强制刷新颜色 */
            }
            if(c->last_hl != (int8_t)hl) {
                c->last_hl = (int8_t)hl;
                lv_obj_set_style_bg_color(
                    c->bubble,
                    hl ? lv_color_hex(HEX_HL_COLOR) : (it != NULL ? it->color : lv_color_black()),
                    LV_PART_MAIN);
            }
```

`hex_cell_t` 追加字段 `int8_t last_hl;`，并在 `lv_hex_menu_create()` 的池初始化里加 `c->last_hl = -1;`。

`hex_menu_ctx_t` 追加 `int32_t focus_slot_prev;`——高亮要用**上一帧**算出的焦点。本帧的 `focus_slot` 要等整个循环跑完才知道，循环内用不了。在 `hex_layout()` 末尾 `m->focus_slot = best_slot;` 之后补一行：

```c
    m->focus_slot_prev = best_slot;
```

并在 `lv_hex_menu_create()` 中初始化 `m->focus_slot_prev = -1;`。

脉冲：在计算 `diam` 之后、判断是否出屏之前插入：

```c
            int32_t diam = (int32_t)lroundf(diam_f);
            if(m->pulse_slot == slot) {
                const uint32_t el = lv_tick_elaps(m->pulse_tick);
                if(el >= HEX_PULSE_MS) {
                    m->pulse_slot = -1;
                }
                else {
                    /* 0 -> 1 -> 0 的三角波，峰值在中点 */
                    const float ph = (float)el / (float)HEX_PULSE_MS;
                    const float amp = (ph < 0.5f) ? (ph * 2.0f) : ((1.0f - ph) * 2.0f);
                    diam = (int32_t)lroundf(diam_f * (1.0f + HEX_PULSE_GAIN * amp));
                }
            }
```

注意原来 `const int32_t diam = ...` 的 `const` 要去掉。

跨格脉冲：在 `hex_layout()` 末尾 `m->focus_slot = best_slot;` 之前插入：

```c
    if(best_slot >= 0 && best_slot != m->focus_slot && m->focus_slot >= 0) {
        m->pulse_slot = best_slot;
        m->pulse_tick = lv_tick_get();
    }
```

- [ ] **Step 3: 实现点击判定**

在 `hex_pointer_cb` 的 `case LV_EVENT_RELEASED:` 中，把 `m->state = HEX_ST_GLIDE;` 替换为：

```c
        case LV_EVENT_RELEASED: {
            const uint32_t held = lv_tick_elaps(m->press_tick);
            if(m->press_travel <= HEX_CLICK_TRAVEL && held <= HEX_CLICK_MS) {
                hex_handle_click(obj, m, p);
                m->vx = 0.0f;
                m->vy = 0.0f;
            }
            m->state = HEX_ST_GLIDE;
            break;
        }
```

在 `hex_pointer_cb` 之前插入命中检测。气泡是圆形，用圆心距离判定而非矩形包围盒，否则边角会误命中相邻气泡：

```c
static void hex_handle_click(lv_obj_t * obj, hex_menu_ctx_t * m, lv_point_t p)
{
    int32_t hit_slot = -1;
    int32_t best_d2 = INT32_MAX;

    for(int i = 0; i < HEX_POOL_SIZE; i++) {
        hex_cell_t * c = &m->pool[i];
        if(c->slot < 0 || lv_obj_has_flag(c->bubble, LV_OBJ_FLAG_HIDDEN)) continue;

        const int32_t rad = c->last_d / 2;
        const int32_t ccx = c->last_x + rad;
        const int32_t ccy = c->last_y + rad;
        const int32_t dx = p.x - ccx;
        const int32_t dy = p.y - ccy;
        const int32_t d2 = dx * dx + dy * dy;

        if(d2 <= rad * rad && d2 < best_d2) {
            best_d2 = d2;
            hit_slot = c->slot;
        }
    }

    if(hit_slot < 0 || m->items == NULL || m->item_cnt == 0) return;

    m->pulse_slot = hit_slot;
    m->pulse_tick = lv_tick_get();

    if(m->user_cb != NULL) {
        const int32_t idx = (int32_t)((uint32_t)hit_slot % m->item_cnt);
        lv_obj_send_event(obj, LV_EVENT_VALUE_CHANGED, (void *)(lv_uintptr_t)idx);
    }
}
```

- [ ] **Step 4: 实现 `lv_hex_menu_set_event_cb`**

整体替换 Task 4 里的占位实现：

```c
void lv_hex_menu_set_event_cb(lv_obj_t * obj, lv_event_cb_t cb, void * user_data)
{
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    m->user_cb = cb;
    m->user_data = user_data;
    lv_obj_add_event_cb(obj, cb, LV_EVENT_VALUE_CHANGED, user_data);
}
```

并在 `lv_hex_menu_create()` 的 ctx 初始化处补 `m->pulse_slot = -1;`。

- [ ] **Step 5: 在 `ui.c` 里挂回调，并把鼠标光标提到最前**

在 `ui.c` 的 `ui_init()` 之前加回调函数：

```c
static void on_item_clicked(lv_event_t * e)
{
    const int32_t idx = (int32_t)(lv_uintptr_t)lv_event_get_param(e);
    if(idx >= 0 && idx < UI_ITEM_CNT) {
        LV_LOG_USER("hex menu clicked: [%d] %s", (int)idx, s_labels[idx]);
        printf("hex menu clicked: [%d] %s\n", (int)idx, s_labels[idx]);
    }
}
```

顶部补 `#include <stdio.h>`。

`ui_init()` 末尾，`lv_hex_menu_set_items(...)` 之后追加：

```c
    lv_hex_menu_set_event_cb(menu, on_item_clicked, NULL);

    /* hal.c 先于菜单创建了鼠标光标对象，会被气泡盖住，提到最前 */
    lv_indev_t * indev = NULL;
    while((indev = lv_indev_get_next(indev)) != NULL) {
        lv_obj_t * cur = lv_indev_get_cursor(indev);
        if(cur != NULL) lv_obj_move_foreground(cur);
    }
```

`lv_indev_get_cursor(lv_indev_t *)` 与 `lv_indev_get_next(lv_indev_t *)` 均已确认存在于本版本（`include/lvgl/indev/lv_indev.h:385`），照上面写即可，无需改 `hal.c`。`lv_indev_get_next(NULL)` 返回第一个输入设备，传入非 NULL 则返回下一个，遍历到 NULL 结束。

- [ ] **Step 6: 编译并肉眼验收**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake --build build -j > /tmp/b.log 2>&1; echo "EXIT=$?"
./bin/main.exe
```

Expected：
- 离屏幕中心最近的气泡显示为橙色 `#FF9500`，拖动时高亮在气泡间平滑传递
- 焦点跨到新格子时，该气泡轻微弹一下
- 单击某个气泡：它弹一下，终端打印 `hex menu clicked: [N] Xxx`
- **拖动后松手不应触发点击**——拖拽超过 8px 就不算点击
- 鼠标光标始终可见，不被气泡遮挡

- [ ] **Step 7: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "feat: 中心高亮、跨格脉冲与点击回调

最近中心项橙色高亮，跨格时触发缩放脉冲。点击按圆心距离命中而非
矩形包围盒；累计位移超过 8px 或按住超过 400ms 判为拖拽不触发点击。"
```

---

### Task 7: 滚轮整体缩放

**Files:**
- Modify: `hex_menu_ui/hex_menu/lv_hex_menu.c`

**Interfaces:**
- Consumes: Task 4 的 `hex_menu_ctx_t.s`
- Produces: 无新增公共 API

- [ ] **Step 1: 加入缩放目标与平滑**

`hex_menu_ctx_t` 追加：

```c
    float s_target;
```

参数区追加：

```c
#define HEX_S_STEP      8.0f    /* 每格滚轮改变的网格尺寸 */
#define HEX_S_LERP      0.18f   /* 每帧向目标逼近的比例 */
```

`lv_hex_menu_create()` 中 `m->s = HEX_S_DEF;` 后补 `m->s_target = HEX_S_DEF;`。

- [ ] **Step 2: 每帧平滑逼近目标尺寸**

在 `hex_physics_update()` 的 `switch` 之前插入。缩放时焦点的世界坐标要按比例跟着变，否则中心那一格会在缩放中漂走：

```c
    if(m->s != m->s_target) {
        const float old_s = m->s;
        m->s += (m->s_target - m->s) * HEX_S_LERP;
        if(fabsf(m->s_target - m->s) < 0.05f) m->s = m->s_target;

        /* 网格整体是按 s 线性缩放的，焦点世界坐标须同比缩放，
         * 否则中心对准的格子会在缩放过程中飘走 */
        const float k = m->s / old_s;
        m->fx *= k;
        m->fy *= k;
        m->target_x *= k;
        m->target_y *= k;
    }
```

这段插在 `switch(m->state)` 之前，不要加 `LV_UNUSED(dt)`——`dt` 在下面的 GLIDE 与 SNAP 分支里已经用到了。

- [ ] **Step 3: 处理滚轮事件**

`lv_sdl_mousewheel_create()` 创建的是 ENCODER 类型输入设备，滚轮转动经 `LV_EVENT_ROTARY` 送达，转动量用 `lv_event_get_rotary_diff()` 取。ENCODER 事件只发给**当前获得焦点的组内对象**，所以必须把菜单加入默认组。

在 `hex_pointer_cb` 之后插入：

```c
static void hex_rotary_cb(lv_event_t * e)
{
    lv_obj_t * obj = lv_event_get_target_obj(e);
    hex_menu_ctx_t * m = ctx_of(obj);
    if(m == NULL) return;

    const int32_t diff = lv_event_get_rotary_diff(e);
    if(diff == 0) return;

    m->s_target -= (float)diff * HEX_S_STEP;
    if(m->s_target < HEX_S_MIN) m->s_target = HEX_S_MIN;
    if(m->s_target > HEX_S_MAX) m->s_target = HEX_S_MAX;
}
```

在 `lv_hex_menu_create()` 的三行指针事件注册之后追加：

```c
    lv_obj_add_event_cb(obj, hex_rotary_cb, LV_EVENT_ROTARY, NULL);

    /* ENCODER 事件只发给组内获焦对象，必须入组并获焦 */
    lv_group_t * g = lv_group_get_default();
    if(g != NULL) {
        lv_group_add_obj(g, obj);
        lv_group_focus_obj(obj);
    }
```

`hal.c` 已经调用过 `lv_group_set_default(lv_group_create())` 并把 mousewheel 设备绑到默认组，所以这里能拿到组。

- [ ] **Step 4: 编译并肉眼验收**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/lv_port_pc_vscode-master"
cmake --build build -j > /tmp/b.log 2>&1; echo "EXIT=$?"
./bin/main.exe
```

Expected：
- 向上滚动滚轮，气泡变密变多（网格尺寸缩到 60）；向下滚动变疏变少（放大到 120）
- 缩放是平滑过渡，不是硬跳
- 缩放过程中，原本在屏幕中心的气泡**始终保持在中心**，不会漂走
- 缩放到最小时仍不掉帧、无气泡缺失（对象池 128 够用）
- 缩放后拖拽、惯性、吸附、高亮、点击全部仍然正常

若缩到最小时屏幕边缘出现空缺（池耗尽），把 `HEX_POOL_SIZE` 提到 160 重编。

- [ ] **Step 5: 全量回归**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
gcc -O2 -Wall -Wextra -Werror -I hex_menu -o test/test_hex_math.exe \
    test/test_hex_math.c hex_menu/hex_grid.c hex_menu/hex_physics.c -lm \
    && ./test/test_hex_math.exe
```

Expected: `ALL TESTS PASSED`（10 个测试全过）。

- [ ] **Step 6: 提交**

```bash
cd "D:/Program_Workspace/GUI_Workspace/LVGL_PRO_Workspace/hex_menu_ui"
git add -A
git commit -m "feat: 滚轮整体缩放

经 LV_EVENT_ROTARY 接收 SDL mousewheel（ENCODER 类型，需入默认组
获焦）。网格尺寸平滑逼近目标值，焦点世界坐标同比缩放以保证中心
气泡在缩放过程中不漂移。"
```

---

## 完成标准

全部任务完成后应满足：

1. `./bin/main.exe` 弹出 800×480 窗口，无任何 LVGL 示例/demo 代码参与
2. 37 个彩色气泡按六边形蜂窝排布，中心放大镜效果明显
3. 二维无界拖拽，任意方向拖不到头，图案无缝重复
4. 松手惯性滑行 + 弹簧吸附，最终有气泡精确停在正中
5. 中心气泡橙色高亮，跨格有脉冲反馈
6. 点击气泡打印日志，拖拽不误触发
7. 滚轮平滑缩放，中心气泡不漂移
8. 满屏连续拖拽稳定 60fps
9. `test_hex_math.exe` 全绿
10. 所有代码 `-Wall -Wextra -Werror` 无警告
